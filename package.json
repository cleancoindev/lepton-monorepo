{
  "name": "root",
  "private": true,
  "scripts": {
    "compile": "tsc -b -v",
    "clean": "rm -rf dist tsconfig.tsbuildinfo && npm run clean -ws"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@railgun-community/lepton-abstract-utils": "*",
    "@railgun-community/lepton-utils": "*"
  },
  "devDependencies": {
    "@types/node": "^17.0.17",
    "lerna": "^4.0.0",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  }
}
